<template>
  <footer>&#xec24;</footer>
</template>

<style lang="less">
footer {
  background: var(--bgcolor);
  color: var(--bgtext);
  padding: var(--vpadding) var(--right) var(--bottom) var(--left);
}
</style>
