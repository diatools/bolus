<template>
  <header>ABC</header>
</template>

<style lang="less">
header {
  background: var(--bgcolor);
  color: var(--bgtext);
  padding: var(--top) var(--right) var(--vpadding) var(--left);
}
</style>
